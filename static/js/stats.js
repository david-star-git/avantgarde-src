function animateCounter(element,target,duration=2000,hasSuffix=false){const start=0;const increment=target/(duration/16);let current=start;const timer=setInterval(()=>{current+=increment;if(current>=target){element.textContent=hasSuffix?target+"+":target;clearInterval(timer);}else{element.textContent=Math.floor(current)+(hasSuffix?"+":"");}},16);}
let animated=false;const observer=new IntersectionObserver((entries)=>{entries.forEach((entry)=>{if(entry.isIntersecting&&!animated){animated=true;document.querySelectorAll(".stat-item").forEach((item,index)=>{const target=parseInt(item.dataset.target);const numberElement=item.querySelector(".stat-number");const hasSuffix=numberElement.textContent.includes("+");setTimeout(()=>{item.classList.add("animated");animateCounter(numberElement,target,2000,hasSuffix,);},index*200);});}});},{threshold:0.2,rootMargin:"0px",},);const statsSection=document.querySelector(".stats");if(statsSection){observer.observe(statsSection);}