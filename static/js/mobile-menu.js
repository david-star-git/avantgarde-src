const button=document.querySelector('.mobile-menu-toggle');const navMenu=document.querySelector('.nav-center');if(button&&navMenu){button.setAttribute('aria-expanded','false');button.addEventListener('click',(e)=>{e.stopPropagation();const isOpened=button.getAttribute('aria-expanded');if(isOpened==='false'){button.setAttribute('aria-expanded','true');navMenu.classList.add('active');}else{button.setAttribute('aria-expanded','false');navMenu.classList.remove('active');}});const navLinks=navMenu.querySelectorAll('.nav-links a');navLinks.forEach(link=>{link.addEventListener('click',()=>{button.setAttribute('aria-expanded','false');navMenu.classList.remove('active');});});document.addEventListener('click',(e)=>{const isOpened=button.getAttribute('aria-expanded')==='true';if(isOpened&&!button.contains(e.target)&&!navMenu.contains(e.target)){button.setAttribute('aria-expanded','false');navMenu.classList.remove('active');}});document.addEventListener('keydown',(e)=>{if(e.key==='Escape'&&button.getAttribute('aria-expanded')==='true'){button.setAttribute('aria-expanded','false');navMenu.classList.remove('active');}});}